# Example .ai/pipeline.conf for a ROS 2 colcon workspace (RX150 project).
AI_PROTECTED_FILES=(tools/build.sh rx150.sh source_all.sh)
: "${AI_AUDIT_FOCUS:=ROS callbacks/executors/threads; 400-500 Hz control-loop timing; layering rules in AGENTS.md; never edits to src/vendor/}"
: "${AI_REPORT_LANG:=Vietnamese}"
